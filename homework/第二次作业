1 (st4(1).4)  已知X=-0.01111，Y=+0.11001，求[X]补，[-X]补，[Y]补，[-Y]补，X+Y=？，X-Y=？


2 (st4)若浮点数X的二进制存储格式为（41360000）16，求其32位浮点数的十进制值(按照IEEE754标准)


3.假设编译器规定int和short类型长度分别为32位和16位，若有下列C语言语句：
        unsigned short x = 65530;
        unsigned int y = x;
    得到y的机器数为多少，用16进制表示。





